import { DragDeltaDirective } from './drag-delta.directive';

describe('DragDeltaDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDeltaDirective();
    expect(directive).toBeTruthy();
  });
});
