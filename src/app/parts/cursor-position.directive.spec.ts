import { CursorPositionDirective } from './cursor-position.directive';

describe('CursorPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorPositionDirective();
    expect(directive).toBeTruthy();
  });
});
